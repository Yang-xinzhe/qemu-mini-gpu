# Buildroot output

This directory is reserved for generated Buildroot artifacts.

Build the root filesystem from the repository root:

```sh
./scripts/build-rootfs.sh
```

The compressed root filesystem will be generated at:

```text
buildroot/build/images/rootfs.cpio.gz
```
