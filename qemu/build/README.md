# QEMU build output

This directory is reserved for generated QEMU build artifacts.

Build QEMU from the repository root:

```sh
./scripts/build-qemu.sh
```

The emulator will be generated at:

```text
qemu/build/qemu-system-x86_64
```
