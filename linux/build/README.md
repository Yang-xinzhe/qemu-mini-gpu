# Linux build output

This directory is reserved for generated Linux kernel build artifacts.

Build the kernel from the repository root:

```sh
./scripts/build-kernel.sh
```

The bootable kernel image will be generated at:

```text
linux/build/arch/x86/boot/bzImage
```
